import { SpeedConverterPipe } from './speed-converter.pipe';

describe('SpeedConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new SpeedConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
